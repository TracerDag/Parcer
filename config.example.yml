env: dev

proxy:
  enabled: false
  url: null
  username: null
  password: null

trading:
  leverage: 1
  max_positions: 1
  fixed_order_size: 10.0

exchanges:
  example_exchange:
    enabled: true
    sandbox: true
    credentials:
      api_key: "your_api_key"
      api_secret: "your_api_secret"
    options:
      some_exchange_specific_flag: true
      rate_limit_per_second: 10
